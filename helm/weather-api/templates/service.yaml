apiVersion: v1
kind: Service
metadata:
  name: {{ include "weather-api.fullname" . }}
  labels:
    {{- include "weather-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "weather-api.selectorLabels" . | nindent 4 }}